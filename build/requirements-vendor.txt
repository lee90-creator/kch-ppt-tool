python-pptx==1.0.2
Flask==3.1.3
PyMuPDF==1.24.3
pillow==12.1.1
# Removed numpy: the web-tool input contract and native export do not require numerical arrays.
mammoth==1.12.0
# Removed openpyxl: spreadsheet input and export are outside the web-tool input contract.
svglib==2.0.2
reportlab==5.0.0
markdownify==1.2.3
beautifulsoup4==4.15.0
requests==2.31.0
lxml==6.0.2
# Removed XlsxWriter: spreadsheet export is not part of the web-tool's native export path.
# Removed edge-tts: text-to-speech is not required for supported input conversion.
# Removed EbookLib: ebook input is outside the supported input contract.
# Removed nbconvert: notebook conversion is outside the supported input contract.
curl_cffi==0.15.0
